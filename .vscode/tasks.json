{
  // See https://go.microsoft.com/fwlink/?LinkId=733558
  // for the documentation about the tasks.json format
  "version": "2.0.0",
  "tasks": [
    {
      "label": "interactive docker",
      "type": "shell",
      "command": "docker run -p 8000:3000 -v ${workspaceFolder}:/local -v ${workspaceFolder}/generated:/generated -it zimbee"
    },
    {
      "label": "build docker",
      "type": "shell",
      "command": "docker build -t zimbee ."
    },
    {
      "type": "npm",
      "script": "start",
      "problemMatcher": [
        "$eslint-compact"
      ],
      "label": "npm: start",
      "detail": "node src/create-index.mjs"
    }
  ]
}